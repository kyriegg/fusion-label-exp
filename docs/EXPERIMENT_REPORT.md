# Function Label 融合实验报告 —— 第一阶段(G1 人形)

> 记录日期: 2026-07-26
> 平台: Isaac Lab 2.3.2 / rsl_rl / Unitree G1 (37 DoF)
> 目标: 验证"冻结模仿 policy × 冻结感知 policy + 可学融合系数 α"方案在物理仿真中的可行性
>
> **主要结论(先行)**: 融合方案的"随地形条件切换"能力**可实现** —— 但须用监督式训练
> (DAgger)配合物理直连信号(height_scan 地形粗糙度),纯 RL(PPO 软奖励)不足以习得。
> 最终 α 网络在真机器人上实现: 平地 α2=0.40(偏模仿保风格)、崎岖 α2=0.80(偏感知保稳定)。

---

## 1. 方案回顾

冻结两套已训练策略,只训练一个轻量 α 网络输出融合系数,生成 hybrid 动作驱动机器人:

```
hybrid = α1 ⊙ label1(模仿) + α2 ⊙ label2(感知)
```

其中 α 网络是唯一可训练模块(作为 PPO 的 actor)。四项约束(相似性/不穿模/不悬空/不抖动)
作为 reward 引导 α 的学习。核心问题: **α 能否学会"平地偏模仿保风格、崎岖偏感知保稳定"的条件切换?**

### 替身设置

真实的模仿/感知网络尚未就绪,本阶段用官方策略作替身验证机制:
- 模仿侧(label1): `Isaac-Velocity-Flat-G1-v0` 训练的平地策略(不懂复杂地形)
- 感知侧(label2): `Isaac-Velocity-Rough-G1-v0` 训练的崎岖策略(带地形高度扫描)

两者结构同构于真实方案,替身实验验证的是**融合机制本身**,而非具体网络。

---

## 2. 专家基线(崎岖地形,256 env × 2000 步)

先刻画两个替身专家各自的能力,作为融合结果的参照上下界:

| 策略 | 平均存活步数 | 摔倒率 | 速度跟踪误差(m/s) | 双脚离地率 |
|---|---|---|---|---|
| flat (纯模仿) | 389.4 | 91.6% | 0.232 | 0.055 |
| rough (纯感知) | 977.1 | 15.1% | 0.237 | 0.029 |
| avg (朴素平均 0.5/0.5) | 850.5 | 46.8% | 0.202 | 0.036 |

**关键观察**:
- 模仿策略在崎岖地形上几乎无法行走(摔倒率 91.6%),量化了"缺乏地形感知"的代价。
- 感知策略在崎岖地形上接近满分(存活 977/1000 步)。
- 两者速度跟踪能力相当(0.232 vs 0.237),差距全在稳定性上——地形感知买到的是"站得住",不是"走得快"。
- 朴素平均在存活上继承了一部分优势,但摔倒率(46.8%)远差于纯感知——**说明简单平均不是好的融合方式**。

---

## 3. 融合实验演进(v1 → v6)

### v1 / v2:α 可控性验证

| 版本 | 关键改动 | α2 均值 | 存活步数 | 摔倒率 |
|---|---|---|---|---|
| v1 | w_sim=1.0(相似性拉力强) | **0.05** | 893 | 36.7% |
| v2 | w_sim=0.3 + 排除手部关节 + 加重稳定性惩罚 | **0.92** | 966 | 18.3% |

**结论一(重要,可复现)**: **α 的走向由四项约束的权重配比决定,且完全可控。**
仅改奖励权重,α2 就从 0.05(几乎纯模仿)干净地翻到 0.92(几乎纯感知),控制关系清晰。
这与 Anymal-C 上的先前实验一致(两种形态重复出现同一规律)。

**副产物发现(关节数据分析)**: G1 的手部关节(five/six 等)对 locomotion 无贡献,
却在两专家间差异巨大(纯噪声),会稀释 α 的优化信号。v2 起将其从相似性奖励中排除。
其中 `six` 关节还存在"两专家目标超出关节行程 → PD 死顶限位空耗力矩"的现象。

### v3:加入地形观测

给 α 网络的观测拼上 187 维地形高度扫描(观测 197→384 维),期望 α 能据地形切换。

结果: 崎岖 α2=0.91、平地 α2=0.90 —— **几乎无差异,未实现切换。**
原因: v3 只在纯崎岖地形上训练,α 从未见过平地,无从学习"平地该回落"。

### v4:切换奖励(DAgger 路径1 · 软监督)

新增第五项 reward: 用下肢 12 关节的两专家分歧 `‖label2−label1‖` 导出目标 α2
(分歧小→平地→目标低;分歧大→崎岖→目标高),惩罚 α2 偏离目标。

结果: `switch/err` 从 0.008 干净降到 0.004(**切换机制本身在工作**),但两地形 α2 仍≈0.87。
诊断发现 `switch/target_a2` 全程是 0.86 的直线 → 训练环境**依然全是崎岖**,平地没进去。

### v5 / v6:根治"从未见过平地"

排查发现根因: **G1 rough 任务的 6 种子地形全部是崎岖的**(台阶/盒子/噪声/斜坡),
无论课程等级多低都不是平地;且地形缓存(use_cache)会把配置改动覆盖回默认。

v6 修复: 重构地形生成器为 **50% 纯平面 + 50% 崎岖**,并关闭缓存。
`[DIAG-after]` 确认地形 `sub_terrains=['flat','stairs','boxes','rough'], use_cache=False` —— **地形终于正确**。

结果(v6, 在真·纯平面上评估):

| 指标 | 数值 |
|---|---|
| α2 均值 | **0.88** (仍未在平地回落) |
| 存活步数 | 998.1 (近满分) |
| 摔倒率 | **2.7%** (全场最佳) |
| 力矩峰值 | 137 (最低) |

---

## 4. DAgger 监督式训练:攻克条件切换

v1-v6 用纯 RL 始终无法习得切换。转向监督式硬训练(DAgger 思路):
让融合策略 rollout 收集状态,每帧算一个"专家目标 α"作标签,直接回归训练 α 网络,
不给 RL 讨价还价的空间。关键在于**选对目标信号**。

### 4.1 信号标定:排除"专家分歧"信号

首先尝试用"下肢两专家分歧 ‖label2−label1‖"作切换信号(分歧小→平地、大→崎岖)。
用真实采集数据标定后发现: **平地与崎岖的分歧分布几乎完全重叠**
(平地75分位=0.487,崎岖25分位=0.388,甚至反向),**无区分度**。

原因: 两专家的分歧主要来自"它们本就是两个不同网络"的基线差异,淹没了地形带来的差异。
这一负结果**回溯解释了 v3-v6 为何学不会切换 —— 一直设想的信号源本身就没有区分力。**

### 4.2 改用 height_scan 地形粗糙度信号

改用**物理直连**信号: height_scan(脚下高度扫描)的标准差作为地形粗糙度。
平地粗糙度小→目标 α2 低(偏模仿);崎岖粗糙度大→目标 α2 高(偏感知)。
此信号来自真实地形几何,不受网络差异污染。

流程(半离线):
1. 在混合地形(50%平面+50%崎岖)上用感知策略 rollout,采集
   (raw_obs 含 height_scan, label1, label2) 共 19.2 万样本。
2. 脱离 Isaac,纯 PyTorch 监督训练 α 网络,目标 α2 由 height_scan 粗糙度映射
   (用数据分位数自动定阈值,绕开 height_scan 的固定基线偏移)。

### 4.3 结果:训练态与端到端均实现切换

**训练态**(监督拟合,100 epoch):

| | 平地样本预测 α2 | 崎岖样本预测 α2 |
|---|---|---|
| ep0 | 0.341 | 0.630 |
| ep99 | **0.261** | **0.927** |

**端到端**(训好的 α 网络接回 Isaac,真机器人真地形):

| 地形 | α2 均值 | 存活步数 | 摔倒率 | 速度跟踪(m/s) | 力矩峰值 | hybrid-label1偏差 |
|---|---|---|---|---|---|---|
| 崎岖 | **0.80** | 975 | 21.3% | 0.234 | 285 | 0.250 |
| 平地 | **0.40** | 1000(满) | **0.4%** | **0.143** | **117** | **0.127** |

**α2 从崎岖 0.80 降到平地 0.40 —— 条件切换实现。** 同一 α 网络据脚下地形自动调节:
崎岖偏感知保稳定、平地偏模仿保风格。且平地上行为指标全面最优(几乎不摔、
速度最准、力矩最省、最接近模仿风格),印证了"平地本就该多听模仿"这一预期。

对比 v1-v6 纯 RL: 无论平地崎岖 α2 恒为 0.88(无切换)。DAgger 一次做到。

---

## 5. 核心结论

### 结论一:α 融合可控
约束权重决定 α 走向,可在"保风格(模仿)"与"保稳定(感知)"间任意滑动。
v1↔v2 的 α2 从 0.05→0.92 是直接证据。两种机器人形态上均复现。

### 结论二:学习式融合优于朴素平均
v2 用 α2≈0.92 的融合,在崎岖上存活 966 步、摔倒率 18.3%,优于朴素平均(850 步 / 46.8%)——
**融合比例应当学习,而非固定为 0.5**。

### 结论三:纯 RL + 软奖励无法习得条件切换
在满足全部前置条件后(v3 加地形观测、v6 确认平地真正进入训练、切换奖励项 `switch/err` 持续下降、
评估使用真·纯平面),α2 在平地与崎岖上仍稳定在 0.88 左右,**未出现随地形的切换**。

排除了所有外部原因(数据/观测/地形/评估),结论指向训练范式本身:
**PPO 的软奖励信号会被其他约束项和课程分布淹没,α 收敛到"全局最优妥协点"而非条件策略。**
由于训练分布以崎岖为主,全程高 α2 的期望回报最高,切换的边际收益盖不过其探索成本。

### 结论四(主要成果):监督式训练 + 物理信号 = 条件切换可实现
将训练范式从"软奖励 RL"换成"监督式硬训练"(DAgger),并把信号源从无区分度的
"专家分歧"换成物理直连的"height_scan 地形粗糙度"后,α 网络**成功学会随地形切换**:
- 训练态: 平地 α2=0.26、崎岖 α2=0.93;
- 端到端(真机器人): 平地 α2=0.40、崎岖 α2=0.80,且平地行为指标全面最优。

**这是第一阶段的核心成果**:它不仅做出了切换,还完整闭合了"为什么之前做不到"的因果链——
问题从不在 α 网络的能力,而在训练范式(需硬监督)与信号选择(需物理直连)。

### 完整逻辑链
1. **α 可控**(v1-v2): 权重决定走向,0.05↔0.92。
2. **纯 RL 无法切换**(v3-v6): 穷尽排除数据/观测/地形/评估,α2 恒为 0.88。
3. **信号诊断**: "专家分歧"信号平地/崎岖分布重叠,无区分度 —— 回溯解释 v3-v6 的失败。
4. **DAgger 攻克**: 监督式训练 + height_scan 粗糙度信号,端到端实现 0.40/0.80 切换。

---

## 5. 已交付产物

- **代码**: 融合环境(`fusion_env_g1.py`)、PPO 训练(`train_isaac_g1.py`)、评估(`eval_policies.py`)、
  专家采集(`collect_expert.py`)、关节分析(`analyze_joints.py`);
  **DAgger(`dagger/`)**: 信号标定(`calibrate_divergence.py`)、数据采集(`collect_dagger_data.py`)、
  离线监督训练(`train_dagger_offline.py`)。
- **数据**: 各版本 fusion / dagger 在平地/崎岖的对比指标(`policy_comparison_*.csv`)、
  逐帧关节运动数据(`joints_*.npz`,含关节位置/速度/加速度/力矩、label1/label2/hybrid、α、脚部接触力)、
  DAgger 训练数据集(`dagger_dataset.npz`)、训好的 α 网络(`dagger_alpha_net.pt`)。
- **训练曲线**: 各版本 TensorBoard 日志(含 `alpha/a2_mean`、`switch/*`)。

---

## 6. 下一步

1. **提升切换锐度**: 当前端到端 α2 分化为 0.40/0.80(训练态为 0.26/0.93,端到端略被压缩)。
   可通过增大训练地形对比度、迭代 DAgger(用当前策略再 rollout 收数据)、或调粗糙度阈值,
   把平地 α2 进一步压低、切换更干脆。
2. **上下半身解耦**(可选): 将标量 α 升级为"上/下半身分组 α",回归方案最初的 per-dim 思想。
   下半身走地形融合,上半身走模仿保风格。需引入上半身动作源方能完整验证。
3. **接入真实网络**: 将替身策略替换为真实的模仿/感知网络,在真实数据上生成 hybrid label,
   作为"数据制作阶段"的产物。切换机制(DAgger + height_scan)可直接复用。
